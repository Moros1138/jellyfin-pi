# jellyfin-pi


## Install Dependencies
```bash
sudo apt install libmpv-dev libcurl4-openssl-dev libgbm-dev libdrm-dev libegl-dev
```

## Building

```bash
cmake -S . -B build
cmake --build build
```
